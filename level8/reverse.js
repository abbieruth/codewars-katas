function solution(str){
    // Convert to array using Array.from(string)
    // Reverse array with array.reverse()
    // Convert back to string using array.join('')?
    // Return reversed string
    return Array.from(str).reverse().join('')
  }